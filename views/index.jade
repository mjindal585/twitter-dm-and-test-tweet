extends layout

block content
  h1 Welcome
  if user
    h2 Dashboard
    p Hello #{user.username}
    img(src=user.photos[0].value)
    br
    a(href="/twitter/tweet") Tweet a test message
    br
    a(href="/twitter/direct/mjindal585") Send a test Direct Message to the developer @mjindal585
    span (will probably fail as he may not be following you)
  else
    a(href='/twitter/login') Login using twitter!